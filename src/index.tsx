import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {App} from "./components/app/app";

ReactDOM.render(
    <App projectName="React Typescript Start"/>,
    document.getElementById('root')
);

// module.hot.accept();
